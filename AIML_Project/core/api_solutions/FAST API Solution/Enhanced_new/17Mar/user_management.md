-- Step 1: Check what roles already exist
SELECT * FROM API_AUTH_ROLES;

-- Step 2: Insert the 4 required roles (skip if already exist)
INSERT INTO API_AUTH_ROLES (ROLE_NAME, DESCRIPTION, IS_ACTIVE, CREATED_DATE)
SELECT 'admin', 'System administrator - full access', 'Y', SYSDATE FROM DUAL
WHERE NOT EXISTS (SELECT 1 FROM API_AUTH_ROLES WHERE ROLE_NAME = 'admin');

INSERT INTO API_AUTH_ROLES (ROLE_NAME, DESCRIPTION, IS_ACTIVE, CREATED_DATE)
SELECT 'performance_engineer', 'Performance engineer - monitoring access', 'Y', SYSDATE FROM DUAL
WHERE NOT EXISTS (SELECT 1 FROM API_AUTH_ROLES WHERE ROLE_NAME = 'performance_engineer');

INSERT INTO API_AUTH_ROLES (ROLE_NAME, DESCRIPTION, IS_ACTIVE, CREATED_DATE)
SELECT 'test_lead', 'Test lead - read and register access', 'Y', SYSDATE FROM DUAL
WHERE NOT EXISTS (SELECT 1 FROM API_AUTH_ROLES WHERE ROLE_NAME = 'test_lead');

INSERT INTO API_AUTH_ROLES (ROLE_NAME, DESCRIPTION, IS_ACTIVE, CREATED_DATE)
SELECT 'viewer', 'Viewer - read only access', 'Y', SYSDATE FROM DUAL
WHERE NOT EXISTS (SELECT 1 FROM API_AUTH_ROLES WHERE ROLE_NAME = 'viewer');

COMMIT;

-- Step 3: Verify
SELECT ROLE_NAME, IS_ACTIVE FROM API_AUTH_ROLES ORDER BY ROLE_NAME;